<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <link href="https://use.fontawesome.com/releases/v5.0.7/css/all.css" rel="stylesheet">
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
        <link href="https://fonts.googleapis.com/css?family=Raleway:300,400,800&display=swap" rel="stylesheet">

        <title>
            {% block title %}
                GEEK'ation - Formulaire de contact
            {% endblock %}
        </title>
    </head>
    <body>
        <div class="bgcontact">

            <h1 class="col-12 mx-auto mt-5 text-center titrelogin">Nous contacter</h1>

            <a href="{{ path('accueil') }}" class="btn btn-warning col-3 d-block mx-auto">Retour à l'accueil</a>
            ​
            <div class="formulaire col-md-6 col-sm-10 col-xs-10 mx-auto m-5 p-3" id="">
                {{form_start(form)}}
                <div class="row">
                    <div class="col-6">
                        {{ form_row(form.prenom) }}
                        {{ form_row(form.nom) }}
                        {{ form_row(form.email) }}
                    </div>

                    <div class="col-6">
                        {{ form_row(form.objet) }}
                        {{ form_row(form.message) }}
                        <div class="form-group">
                            <button class="btn btn-info" type="submit" action="">Envoyer !</button>
                        </div>
                    </div>
                </div>


                {{form_end(form)}}

            </div>
        </div>
    </body>

</html>
